# 安全与隐私

本工具在本机保存账户登录凭据。只添加你拥有或获授权使用的账户；不要向公开仓库、Issue 或截图中上传 `auth.json`、令牌、密码、`.dpapi` 数据文件和含账户信息的完整日志。

本机账户库位于 `%LOCALAPPDATA%\CodexQuotaViewer`，不属于项目源码。Windows DPAPI 加密用于保护静态存储，不能阻止以同一 Windows 用户身份运行的其他程序访问该用户的数据。

服务只监听回环地址，配额请求只发送到固定上游端点。不要把本机端口暴露到公网。导入模式不会使用源客户端的刷新令牌；独立登录模式自行管理令牌生命周期。

问题报告请提供脱敏后的错误类型、HTTP 状态、Python/Windows 版本和复现步骤。若发现可能泄漏凭据的问题，请优先使用仓库支持的私密漏洞报告渠道，不要公开上传可利用细节或真实凭据。
